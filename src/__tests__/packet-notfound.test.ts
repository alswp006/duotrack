import { describe, it, expect } from "vitest";

// Actual NotFound page tests moved to packet-notfound.test.tsx — this file
// used a .ts extension while containing JSX, which tsc cannot parse.
describe("packet-notfound.test.ts (moved)", () => {
  it("superseded by packet-notfound.test.tsx", () => {
    expect(true).toBe(true);
  });
});
